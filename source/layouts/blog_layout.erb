<% wrap_layout :layout do %>

  <header class="post-header" style="background-image: url(<%= image_path(current_page.data.mood_image || "moods/defacto.jpg") %>)">
    <div class="row">
      <h1><%= t("blog.title") %></h1>
    </div>
  </header>

  <%= yield %>

<% end %>
